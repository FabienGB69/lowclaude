Create a new git branch. If a name is provided use it, otherwise generate a descriptive name based on the current task.

Run: git checkout -b $ARGUMENTS

If no argument given, suggest a branch name based on recent work and ask for confirmation before creating it.
