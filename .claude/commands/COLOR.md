Set the prompt bar color for this session.

Usage: /COLOR [color]
Example: /COLOR blue, /COLOR #ff6b6b, /COLOR purple

Inform the user that session color can be set via /config in the Claude Code settings panel, or by using the --color flag when starting a session. Show available color options.
